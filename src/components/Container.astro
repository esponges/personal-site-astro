---
interface Props {
  textCenter?: boolean;
}

const { textCenter = true } = Astro.props;
---

<div class:list={[
  "container mx-auto px-4 sm:px-6 lg:px-8",
  { "text-center": textCenter }
]}>
  <slot />
</div>
